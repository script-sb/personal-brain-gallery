# brain-core

This folder holds the conceptual core service design. The runnable Python package currently lives in `brain_core/` so it can be invoked with:

```bash
python3 -m brain_core status
```

Keep core responsibilities narrow: task state, permission gates, routing, run logs, and orchestration.

