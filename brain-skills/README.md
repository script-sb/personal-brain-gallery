# brain-skills

Skill subsystem.

A skill should declare:

- name
- description
- triggers
- inputs
- outputs
- permission level
- tool wrapper
- failure behavior

Use `browserbase/skills` and `hello-agents` as references, but keep local-first execution.

